# OmniDoc — Unified Document Ingestion
# Combines best of OmniSense, cadAI, and F1

# ── Core ──
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-huggingface>=0.0.3

# ── Embeddings ──
sentence-transformers>=2.2.2
open-clip-torch>=2.20.0
torch>=2.0.0

# ── PDF processing ──
PyMuPDF>=1.23.0
pdfplumber>=0.10.0
camelot-py[cv]>=0.11.0       # optional — requires ghostscript system package
Pillow>=10.0.0

# ── Document loaders (LangChain) ──
pypdf>=3.17.0
unstructured>=0.10.0

# ── Vision API ──
groq>=0.9.0

# ── Storage ──
pymongo>=4.5.0
qdrant-client>=1.7.0

# ── Utilities ──
numpy<2.0.0
python-dotenv>=1.0.0
